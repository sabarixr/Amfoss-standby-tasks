1. Which of the following are operators, and which are values?

*
'hello'
-88.8
-
/
+
5


Answer : 
	* is an operator.
	'hello' is a string.
	-88.8 float value.
	- subtraction operation.
	/ is division operator.
	+ this is addition operator.
	5 is an integer value.
	
2. Which of the following is a variable, and which is a string?

 spam
'spam'

Answer:
       span is a variable and 'span' is a string.
       
3. Name three data types.

Answer: 
	int   - integer.
	str   - string.
	float - floating point numbers.

4. What is an expression made up of? What do all expressions do?

Answer:
	Expressions are made up of two or more variables or values which have operations performed on them and made into a single result in return.


	from this chapter I can conclude that :
		~ expressions is used to compute a result, manipulate data, building the code to do operation.
		
5. This chapter introduced assignment statements, like spam = 10. What is the difference between an expression and a statement?

Answer: 
	|	Expression		|  	    statements          |
	-----------------------------------------------------------------
	| expression return a result   | it does not return any result |
	-----------------------------------------------------------------
	| expression should at least    | statements are instructions   |
	| have 1 operator and 2 values. | for the computer              |
	-----------------------------------------------------------------
	| (x+5)/2, 2*5  are             | spam = 10 is a statement where|
	|  expressions.                 | value 10 is being assigned to |
	|                               | variable spam                 |
	-----------------------------------------------------------------
	
6. What does the variable bacon contain after the following code runs?

bacon = 20
bacon + 1

Answer:
	The value will be 20, reason being the second line is not assigning the updated value to variable bacon.
	
7. What should the following two expressions evaluate to?

'spam' + 'spamspam'
'spam' * 3

Answer:
 	both returns the same output.
 	'spam' + 'spamspam' : combine both of the strings resulting spamspamspam.
 	'spam' * 3 : here the string is repeated 3 times resulting spamspamspam.

8. Why is eggs a valid variable name while 100 is invalid?
 
Answer:
	eggs is a valid variable name as it follows the rules for naming a variable.
	100 is invalid cause variable name can't start with a number.

9. What three functions can be used to get the integer, floating-point number, or string version of a value?

Answer:
	for integer :  int(value)
	for float   :  float(value)
	for string  :  str(value) 
	
10. Why does this expression cause an error? How can you fix it?

'I have eaten ' + 99 + ' burritos.'

Answer:
	Its invalid because 'I have eaten ' and ' burritos.' are string and 99 is an integer and python \
	cant concatenate string and integer.
	
	To make it right:
			'I have eaten ' + str(99) + ' burritos.'






 	

 	
	
	

